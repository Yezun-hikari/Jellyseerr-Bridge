version: '3.8'

services:
  jellyseerr-bridge:
    build: .
    container_name: jellyseerr-bridge
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - media-network

networks:
  media-network:
    external: true
