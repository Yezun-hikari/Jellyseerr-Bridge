version: '3.8'

services:
  jellyseerr-bridge:
    build: .
    container_name: jellyseerr-bridge
    restart: unless-stopped
    ports:
      - "9800:9800"
    env_file:
      - .env
    networks:
      - media-network

networks:
  media-network:
    external: true
